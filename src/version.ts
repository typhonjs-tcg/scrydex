/**
 * Defines the CLI and schema version for generated JSON card DBs.
 */
export const VERSION: Readonly<{ package: string, schema: string }> = Object.freeze({
   package: '0.1.0',
   schema: '0.0.0'
});
